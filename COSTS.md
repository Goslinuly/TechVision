# Структура затрат (Cost Structure) — §5 ТЗ

Цель — показать две вещи честно:

1. **Текущая конфигурация** (то, что крутится на проде и что вы увидите в
   `/health`) стоит копейки: оркестратор — Groq · Llama 3.3 70B, на бесплатном
   tier — буквально $0.
2. **Если качество потребует фронтир-модели** (Claude Fable 5 + Haiku 4.5), цена
   разбора вырастает до ≈$0.10 — и проект всё равно не разоряется, потому что
   архитектурные митигации (§3) не зависят от провайдера: один вызов
   оркестратора на сообщение, дешёвый подшаг на извлечение claim'ов, локальные
   поиск/эмбеддинги, кэш вердиктов.

Вилка ниже — не слабость, а ответ на вопрос «а если ваш бесплатный лимит
кончится / если понадобится модель посильнее». Смена провайдера — одна
переменная `LLM_PROVIDER`, архитектура и контракт `Card` не меняются
(`app/services/llm.py`, `app/config.py::active_provider`).

## Цены API (за 1M токенов)

**Текущая конфигурация (прод):**

| Модель | Роль | Input | Output |
|---|---|---|---|
| Groq · Llama 3.3 70B (`llama-3.3-70b-versatile`) | оркестратор (tool-use) + подшаги | $0.59 | $0.79 |

На бесплатном tier Groq — **$0** при соблюдении rate limits; цены выше — это
платный tier, т.е. «сколько это стоило бы без бесплатного лимита».

**Конфигурация на фронтир-модели (`LLM_PROVIDER=anthropic`):**

| Модель | Роль | Input | Output |
|---|---|---|---|
| Claude Fable 5 | оркестратор (маршрутизация + синтез) | $10.00 | $50.00 |
| Claude Haiku 4.5 | классификация + извлечение claim'ов | $1.00 | $5.00 |

> Проверять актуальность цен (Groq и Anthropic) в консолях провайдеров перед
> защитой — в этой таблице снимок на момент сдачи.

## Оценка на одно сообщение

Типичное пересланное сообщение — короткое (≈150–400 токенов). Один и тот же
объём работы в обеих конфигурациях: подшаг ≈500 in / 300 out, оркестратор 3–5
tool-use итераций ≈4 000 in / 1 200 out.

| Шаг | ~Input | ~Output | Текущая (Groq) | Фронтир (Fable 5 + Haiku) |
|---|---|---|---|---|
| Классификация + декомпозиция claim'ов | 500 | 300 | ~$0.0005 | ~$0.0020 |
| Оркестратор: 3–5 tool-use итераций | 4 000 | 1 200 | ~$0.0033 | ~$0.1000 |
| **Итого на сообщение** | 4 500 | 1 500 | **≈ $0.004** | **≈ $0.10** |
| | | | (на free tier — **$0**) | |

Разница — **порядка ×25** (точнее ≈×27). Эмбеддинги и семантический поиск в обеих конфигурациях —
локально, **$0** за API.

Порядок величин на объёме (текущая конфигурация, платный tier):

| Разборов / мес | Текущая (Groq) | Фронтир |
|---|---|---|
| 1 000 | ~$4 | ~$100 |
| 10 000 | ~$38 | ~$1 000 |
| 100 000 | ~$380 | ~$10 000 |

Кэш вердиктов и повторяемость фейков срезают это ещё сильнее: одни и те же
сообщения ходят по кругу, второй разбор одного и того же утверждения API не
оплачивается.

## Митигации (уже в коде)

- **Один** вызов оркестратора на сообщение — только маршрутизация и синтез
  (`services/llm.py::run_tool_loop`).
- Извлечение claim'ов и классификация → дешёвый подшаг
  (`services/llm.py::complete_json`; на Anthropic — Haiku, на Groq — та же
  Llama с `response_format: json_object`).
- Поиск / эмбеддинги → локально, без API (`services/embeddings.py`,
  `tools/local_corpus.py`).
- **Кэш вердиктов по хешу утверждения** (`services/cache.py`): одни и те же
  фейки ходят по кругу, повторный разбор не оплачивается.
- **Детерминированный путь** при сбое или исчерпании лимита LLM
  (`orchestrator.py`, ветки `except`) — сервис продолжает отвечать при цене
  API $0, на реальных Google/локальных инструментах.
- Prompt caching системного промпта оркестратора (можно включить далее) —
  ~0.1× цены input на повторных вызовах в конфигурации Anthropic.

## Хостинг

- Railway / Hetzner VPS — self-hosted FastAPI + бот. Порядок $5–20/мес.
- KazLLM-8B — self-hosted (llama.cpp), только вычисления, без API-платы.

Итог по текущей конфигурации: **хостинг — основная статья расходов, а не LLM.**

## Устойчивость (соцпроект)

- Измеряемый эффект собирается в `/metrics` (кол-во проверок, распределение
  вердиктов, найденные манипуляции) — база для метрики «% сообщений, где
  пользователь изменил решение делиться».
- Монетизация при CC-BY-NC: гранты (Medianet/Soros), B2G-пилот с акиматами по
  медиаграмотности, white-label для школьных медиа; коммерческая версия — на
  модели без NC-лицензии (roadmap-слайд). Ограничение CC-BY-NC относится к
  весам KazLLM-8B, не к нашему коду (наш код — MIT, см. `LICENSE`).
